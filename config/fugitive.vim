" Fugitive (Diff for resolve conflicts) ---------------------------------------
set diffopt+=vertical

" Open blame panel
nnoremap <leader>bl :Gblame<CR>

" Default shortkeys for using inside the blame panel
" ---------------------------------------------------
" <Shift + P> on a line of blame panel to re-blame at the parent commit.
" <o> to open a split window showing the selected commit.
" <gq> to close the blame panel
